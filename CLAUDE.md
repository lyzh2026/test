# Claude Code 行为指导原则

本文件基于 Andrej Karpathy 对 LLM 编程助手的观察与实践总结，旨在优化 Claude Code 的工作方式，减少常见错误，提高代码质量。

---

## 核心原则

### 1. Think Before Coding（编码前思考）
**不要假设；明确权衡，遇到不清楚的地方主动询问。**

**具体实践**：
- **明确假设**：在执行前，清楚列出你的假设条件。
- **提供选项**：对于模糊需求，提供 2~3 种可能的理解方式供用户选择。
- **请求澄清**：当需求不明确或存在多种实现方式时，主动询问用户偏好。
- **评估复杂度**：预估任务难度，对复杂任务建议分阶段实施。
- **识别依赖**：分析任务依赖关系，确定合理的执行顺序。

**示例**：
```
❌ 错误：直接开始编写代码
✅ 正确："这个功能有几种实现方式：A 方案简单但扩展性差，B 方案复杂但性能好。您倾向于哪种？"
```

---

### 2. Simplicity First（简单优先）
**编写最少的代码，避免过度工程化，拒绝"为未来准备"的功能。**

**具体实践**：
- **最小实现**：只实现当前需求，不添加"可能有用"的功能。
- **避免过早抽象**：三行相似的代码比一个过早的抽象更好。
- **信任框架**：依赖现有框架的保证，不添加额外的防御性代码。
- **删除无用代码**：如果确定某段代码无用，直接删除而非注释。
- **避免重构**：除非任务明确要求，否则不重构相邻代码。

**示例**：
```
❌ 错误：为简单的数据转换创建抽象工厂模式
✅ 正确：直接写三行 if-else 语句
```

---

### 3. Surgical Changes（精准修改）
**只修改请求的部分，不碰相邻代码，严格匹配现有风格。**

**具体实践**：
- **限定范围**：修改范围严格限定在任务描述的区域。
- **保持风格**：完全匹配项目的代码风格（缩进、命名、注释等）。
- **不"改进"代码**：除非明确要求，否则不优化相邻代码的性能或可读性。
- **避免清理**：不删除未使用的变量，不添加不必要的注释。
- **尊重现状**：即使看到潜在问题，只要不是当前任务范围，就不修改。

**示例**：
```
❌ 错误：修复 bug 时顺便"改进"了函数的命名和结构
✅ 正确：只修改导致 bug 的那一行代码
```

---

### 4. Goal-Driven Execution（目标驱动执行）
**将任务框架化为可验证的目标，而非模糊的指令。**

**具体实践**：
- **定义成功标准**：明确说明如何验证任务完成。
- **测试驱动**：先定义测试方法，再实现功能。
- **验证结果**：实现后立即验证是否满足所有要求。
- **提供检查点**：对于复杂任务，设置中间检查点。
- **确认完成**：完成任务后，明确报告验证结果。

**示例**：
```
❌ 错误："添加一个搜索功能"
✅ 正确："添加搜索功能：1）用户在输入框输入关键词 2）实时显示匹配结果 3）点击结果跳转到详情页。完成后在浏览器中测试搜索流程。"
```

---

## Karpathy 实践洞察

### 关于 LLM 助手的常见缺陷
- **最危险的错误**：模型会替你做出错误假设，并毫不怀疑地继续执行。它们不会管理困惑，不会寻求澄清，不会提出权衡，不会在应该拒绝时拒绝。
- **过度复杂化**：模型倾向于膨胀抽象、堆砌 API、不清理死代码。它们可能用 1000 行代码实现一个低效脆弱的构造，而你只需反问"能不能更简单？"就能被压缩到 100 行。
- **副作用污染**：有时会修改或删除它们不喜欢、或不够理解的无关注释和代码，即使这些内容与当前任务正交。

### 关于有效协作的建议
- **声明式优于命令式**：不要告诉它"做什么"，而是给出**成功标准**，让它循环直到满足目标。
- **先写测试**：让模型先写测试，再实现功能并通过测试。
- **先朴素后优化**：先写出高概率正确的朴素算法，再要求它在保持正确性的前提下优化。
- **利用韧性**：模型不会疲倦或气馁，可以让它在困难问题上持续尝试 30 分钟，这是人类往往做不到的长期耐力。

### 关于个人技能
- **手动编码能力可能萎缩**：由于大量依赖生成，手写代码的细节能力会下降，但代码审读（判别）能力通常保留得更好。

---

## 编码工作流指南

### 收到任务时
1. **分析需求**：应用"编码前思考"，明确任务范围和假设。
2. **规划方案**：考虑最简单直接的实现方式。
3. **确认理解**：如有歧义，立即请求澄清。

### 实施过程中
1. **精准修改**：只修改必要部分，保持代码风格一致。
2. **简单实现**：避免过度设计和过早优化。
3. **持续验证**：边实现边测试，确保符合目标。

### 完成任务后
1. **全面测试**：按照"目标驱动执行"验证所有要求。
2. **清理收尾**：只清理本次修改产生的临时内容。
3. **文档记录**：简洁记录修改内容，不添加多余注释。

### 特殊场景
- **遇到 Bug**：最小化复现 → 精准定位 → 最小化修复（只改导致 bug 的代码行）。
- **性能优化**：先测量 → 针对性优化 → 验证效果。
- **代码审查**：关注功能正确性，忽略与任务无关的风格问题，改进建议标注为"可选"。

---

## 路径与资源引用规范

### 1. 禁止绝对路径
- 严禁在代码中使用任何包含盘符（如 `C:\`, `D:\`）或本地用户路径（如 `/Users/xxx/`）的绝对路径。
- 所有内部文件引用必须使用**相对路径**（例如 `./scripts/game.js`）。

### 2. 资源引用策略
- **外部库**：优先使用稳定的 CDN 链接（如 JSDelivr、Unpkg）引用第三方库，确保无需配置环境即可运行。
- **图片与媒体**：
  - 尽量使用占位图服务或合法的网络图片 URL。
  - 若必须使用本地资源，统一放置在 `./assets/` 目录下，并确保通过相对路径引用。
  - 对于简单图标或小素材，优先考虑使用 **Base64 编码** 直接嵌入，或使用 **SVG** 矢量代码。

### 3. 便携性检查
- 生成的 HTML/Web 项目必须保证"开箱即用"。
- 确保 `index.html` 是入口文件，且所有依赖项在解压后的相对位置保持一致。
- 避免使用需要特定本地服务器环境才能运行的特性，除非同时提供了简易启动脚本。

### 4. 交互调试提示
- 在输出代码前，自检是否存在 `file:///` 类型的硬编码链接。



## 技能调用指南 (Skills Directory)
- **位置**：项目根目录下的 `/skills` 文件夹。
- **调用逻辑**：在执行任何任务前，请检索 `/skills` 目录下是否存在与当前任务匹配的 `.md` 文件。如果匹配，请严格遵循该文件定义的 SOP（标准作业程序）执行任务。

---

## 前端设计规范

本项目的设计系统定义在项目根目录的 `DESIGN.md` 文件中。

### 设计系统
- **参考风格**：Vercel（Geist 设计系统）— 极简、开发者工具风格
- **字体**：Geist Sans（展示级负字距 -2.4px 到 -2.88px）、Geist Mono（代码）
- **核心颜色**：`#171717`（主文本）、`#ffffff`（背景）、`#0072f5`（链接蓝）
- **边框技术**：使用 `box-shadow` 替代传统 border，实现平滑过渡
- 所有前端开发任务应先读取 `DESIGN.md`，再生成代码

### 设计 Skill（Impeccable）
当涉及 UI 设计、重构、打磨、审计、配色、动效等前端任务时，调用 Impeccable skill 执行，支持以下命令：
- `/impeccable audit` — 审计 UI 缺陷和反模式
- `/impeccable polish` — 打磨现有界面
- `/impeccable critique` — 设计评审
- `/impeccable colorize` — 配色方案优化
- `/impeccable typeset` — 排版优化
- `/impeccable animate` — 动效设计
- `/impeccable distill` — 简化/提炼设计